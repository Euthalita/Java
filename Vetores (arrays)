/******************************************************************************
No Java, não é possível misturar tipos(int, float, string) dentro do Array
A declaração do array, se dá pela declaração do tipo do array, colchetes e o nome
do array
*******************************************************************************/
public class Main
{
	public static void main(String[] args) {
	    /*É necessário identificar quantos números será guardado no array, declarando o tipo (new int) 
	    e a quantidade dentro do [] que está apos o new.
	    O array guarda somente a quantidade de valor informado, no exemplo abaixo, são cinco, não pode 
	    acrescentar mais valores á esse array.
	    Podemos também informar a quantidade de valor informado dentro de uma variável, assim fica mais
	    fácil trocar, caso seja uma código muito grande.
	    
	    O exemplo abaixo, o array recebe 5 valores e cada valor contém uma posição, começando do 0
	    */
	   	int[] numeros = new int[5];
	   	String[] letras = new String[5];
	   	
	   	numeros[0] = 3;
	   	numeros[1] = 4;
	   	numeros[2] = 5;
		numeros[3] = 6;
		numeros[4] = 6;

		//Exemplo guardando a quantidade de valores que será armazenado em um array com uma variável
		int num = 5;
		int[] numeros = new int[num]; 
	
		//Dessa forma, é informado o endereço de memória em que o array está armazenado
		System.out.println(numeros);
		
		//Para saber a quantidade de valores informado no array, usa-se o length 
			for(int i=0; i< numeros.length; i++){
		        System.out.println(numeros[i]);
		}
		System.out.println("\n"); //Pula linha
	
		letras[0]= "A";
		letras[1]= "B";
		letras[2]= "C";
		letras[3]= "D";
		letras[4]= "E";
		    for(int i=0; i< letras.length; i++){
		        System.out.println(letras[i]);
		    }
   
   /* Outra forma de adicionar valores ao array*/
    int [] valores = {9,7,6,5};
	}
}
